#include<iostream>
#include<vector>
#include<algorithm>


using namespace std;
int main()
{  
   int a;
   int count=0;
   vector<int> arr={1,2,2};
   cout<<"Enter the value of the energy : ";
   cin>>a;
   sort(arr.begin(),arr.end());
   for(int i=arr.size()-1;i>=0 ;i--)
   {
      // cout<<arr[i];
      // continue;
     if(a>0)
     {
     a=a-arr[i]*2;
     count=count+1;
     }
     else
     {
     
      break;
     }

   }
   if(a<=0)
   {
      cout<<count;
   }
   else
   {
      cout<<"-1";
   }
   return 0;
}
