#include<iostream>
#include<set>
using namespace std;
int main()
{
   set<int> s;
   s.insert(2);
   s.insert(55);
   s.insert(10);
   s.insert(1);
   s.insert(55);
   s.insert(11);
   s.insert(110);



   for(int i:s)
   {
      cout<< i<<" ";
   }
   cout<<endl;
   // s.pop();
   s.erase(55);
   // s.pop(1);
   for(int i:s)
   {
      cout<< i<<" ";
   }
   cout<<s.count(0);

   // 
   set<int>::iterator it=s.find(10);
   for(auto itr=it;itr!=s.end();itr++)
   {
      cout<<*itr<<" ";

   }
   cout<<endl;


   return 0;
}
